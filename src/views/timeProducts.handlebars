<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@10"></script>
</head>
<body>
    
<div class="container">
    {{!-- Lista de productos con websocket --}}
    <div class ="infoProd">
        <h1>Mi  lista de productos;</h1>
            <div class="detalleProd">
            <table>
            <thead>
                <tr>
                    <th>Título</th>
                    <th>Thumbnails</th>
                    <th>Descripción</th>
                    <th>Código</th>
                    <th>Precio</th>
                    <th>Estado</th>
                    <th>Stock</th>
                    <th>Categoría</th>
                    <th>Thumbnails</th>
                </tr>
            </thead>
            <tbody>
                {{#each totalProd}}
                    <tr>
                        <td>{{this.title}}</td>
                        <td>{{this.thumbnails}}</td>
                        <td>{{this.description}}</td>
                        <td>{{this.code}}</td>
                        <td>{{this.price}}</td>
                        <td>{{this.status}}</td>
                        <td>{{this.stock}}</td>
                        <td>{{this.category}}</td>
                        <td><img src="{{this.thumbnails}}" alt=""></td>
                    </tr>
                {{/each}}
            </tbody>
        </table>
            </div>
        </div>
    <div class="formulario">
        <h1>Formulario de Productos</h1>
        <form id="prod-form">
            <div class="inputs">
                <div> 
                <input type="text" id="title" autocomplete="off" placeholder="Ingrese el Titulo" required>
                <input type="text" id="desc" autocomplete="off" placeholder="Ingrese la Descripcion" required>
                <input type="text" id="cod" autocomplete="off" placeholder="Ingrese el Codigo" required>
                <input type="text" id="price" autocomplete="off" placeholder="Ingrese el Precio" required>
            </div>
            <div>
                <input type="text" id="status" autocomplete="off" placeholder="Ingrese el Status" required>
                <input type="text" id="stock" autocomplete="off" placeholder="Ingrese el Stock" required>
                <input type="text" id="cat" autocomplete="off" placeholder="Ingrese la Categoria" required>
                <input type="text" id="img" autocomplete="off" placeholder="Ingrese laImagen" required>
            </div>
            </div>
            <div class="botones">
                <button type="submit">Crear</button>
            </div>    
        </form>
    </div>
</div>
<script src="/socket.io/socket.io.js"></script>
<script src="/js/TimeProducts.js"></script>
</body>